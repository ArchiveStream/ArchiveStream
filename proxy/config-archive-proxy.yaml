
# record, archive
mode: 'record'
archivestream-path: '/opt/archivestream'

# paths:
recorded: 'recorded.yaml'
ignored-urls: 'http_records/ignored-urls.yaml'
tagged-urls: 'http_records/tagged-urls.yaml'
archivestream-path: '/home/user/archivestream/'


